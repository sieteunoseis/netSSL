import React from "react";

const BackgroundLogo = () => {
  return (
    <div className="fixed inset-0 pointer-events-none z-0 bg-grid-pattern opacity-40 dark:opacity-20" />
  );
};

export default BackgroundLogo;
